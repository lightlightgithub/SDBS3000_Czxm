<UserControl x:Class="SDBS3000.Ucs.FontSpace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <UserControl.Resources>
        <FontFamily x:Key="WriteBrush">/SDBS3000.Ucs;Component/Family/#Montserrat Ace</FontFamily>
    </UserControl.Resources>
    <Grid>
        <!--FontWeight="Light"-->
        <ItemsControl Grid.Row="1" x:Name="itemCtrl" ItemsSource="{Binding Ctx, RelativeSource={RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}}" 
           FontWeight="UltraLight"
                      FontSize="{Binding Fts, RelativeSource={RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}}" Foreground="{DynamicResource txtColor1}" HorizontalAlignment="Center" FontFamily="{StaticResource WriteBrush}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="{Binding Thickness, RelativeSource={RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}}">
                        <ContentPresenter Content="{Binding}"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
