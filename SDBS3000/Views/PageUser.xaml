<Page x:Class="SDBS3000.Views.PageUser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:loc="clr-namespace:SDBS3000.Resources"
      xmlns:local="clr-namespace:SDBS3000.Views"
      xmlns:converters="clr-namespace:SDBS3000.Utils.Converters"
      xmlns:uc1="clr-namespace:SDBS3000.Ucs.Controls;assembly=SDBS3000.Ucs"
      DataContext="{Binding Source={StaticResource Locator},Path=UserManagement}"
      mc:Ignorable="d" 
      d:DesignHeight="880" d:DesignWidth="1920"
      Title="PageUser">
    <Page.Resources>
        <converters:UserPermission x:Key="up"/>
        <!--<ImageBrush x:Key="userwxz" ImageSource="/SDBS3000;component/Utils/Image/Measure/按钮选中背景.png"/>-->
        <Style x:Key="DataGridStyle1" TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="RowDetailsVisibilityMode" Value="VisibleWhenSelected"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalOnly"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGrid}">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <Grid>
                                <Image Source="/SDBS3000;component/Utils/Image/User/信息框.png" Stretch="Fill"/>
                                <ScrollViewer x:Name="DG_ScrollViewer" Focusable="false">
                                    <ScrollViewer.Template>
                                        <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="0"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="0.01"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <!--<Button Command="{x:Static DataGrid.SelectAllCommand}" Focusable="false" Style="{DynamicResource {ComponentResourceKey ResourceId=DataGridSelectAllButtonStyle, TypeInTargetAssembly={x:Type DataGrid}}}" Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.All}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>-->
                                                <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter" Grid.Column="1" Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                                                <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" Grid.ColumnSpan="2" Grid.Row="1"/>
                                                <ScrollBar Foreground="Transparent" x:Name="PART_VerticalScrollBar" Grid.Column="2" Maximum="{TemplateBinding ScrollableHeight}" Orientation="Vertical" Grid.Row="1" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}"/>
                                                <Grid Grid.Column="1" Grid.Row="2">
                                                    <Grid.ColumnDefinitions>
                                                        <!--<ColumnDefinition Width="{Binding NonFrozenColumnsViewportHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>-->
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Column="1" Maximum="{TemplateBinding ScrollableWidth}" Orientation="Horizontal" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}"/>
                                                </Grid>
                                            </Grid>
                                        </ControlTemplate>
                                    </ScrollViewer.Template>
                                    <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsGrouping" Value="true"/>
                        <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
        
    </Page.Resources>
    <Page.InputBindings>
        <KeyBinding Modifiers="Ctrl" Key="A" Command="{Binding Add}"/>
        <KeyBinding Modifiers="Ctrl" Key="F" Command="{Binding Modify}"/>
        <KeyBinding Modifiers="Ctrl" Key="S" Command="{Binding Save}"/>
        <KeyBinding Modifiers="Ctrl" Key="D" Command="{Binding Delete}"/>
    </Page.InputBindings>
    <Grid>
        <Border x:Name="main" Width="1730" Height="680" CornerRadius="20">
            <Border.Background>
                <ImageBrush ImageSource="{DynamicResource mainbk}"/>
            </Border.Background>
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40*"/>
                    <ColumnDefinition Width="571*"/>
                    <ColumnDefinition Width="58*"/>
                    <ColumnDefinition Width="780*"/>
                    <ColumnDefinition Width="75*"/>
                    <ColumnDefinition Width="195"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="1" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40*"/>
                        <RowDefinition Height="610*" x:Name="r1"/>
                        <RowDefinition Height="40*"/>
                    </Grid.RowDefinitions>
                    <DataGrid x:Name="ddd" Height="590" Grid.Row="1" Style="{StaticResource DataGridStyle1}" ItemsSource="{Binding Users,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                        AutoGenerateColumns="False" HorizontalAlignment="Left" HeadersVisibility="All" VerticalAlignment="Top" CanUserAddRows="False" 
                        SelectedItem="{Binding Path=User,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,NotifyOnTargetUpdated=True}" Background="Transparent" RowHeaderWidth="0"
                        GridLinesVisibility="None" BorderThickness="0" SelectionMode="Single"  VerticalGridLinesBrush="Transparent" IsReadOnly="True" Margin="-2,0,0,0" SelectionUnit="FullRow" Focusable="True">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
                                <Setter Property="HorizontalContentAlignment" Value="Left"></Setter>
                                <Setter Property="Background" Value="Transparent"></Setter>
                                <Setter Property="Foreground" Value="{DynamicResource RecColor}"/>
                                <Setter Property="Height" Value="80"/>
                                <Setter Property="FontSize" Value="24"/>
                                <Setter Property="Padding" Value="45,5,0,0"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Style.Triggers>
                                    <Trigger Property="DataGridCell.IsSelected" Value="True">
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Grid.Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="DataGridCell.IsSelected" Value="false">
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <Trigger Property="DataGridRow.IsSelected"  Value="True">
                                        <Setter Property="Background"  Value="{StaticResource usertableselect}"/>
                                    </Trigger>
                                    <Trigger Property="DataGridRow.IsSelected"  Value="False">
                                        <Setter Property="Background"  Value="Transparent"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn x:Name="d1" Width="*"  Binding="{Binding NAME}" Header="{Binding [User Name], Source={x:Static loc:LanguageManager.Instance}}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" >
                                        <Setter Property="HorizontalAlignment" Value="Left" />
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontSize" Value="24"/>
                                        <Setter Property="Foreground" Value="{DynamicResource txtColor1}"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Padding" Value="45,25,0,0"/>
                                        <Setter Property="LineHeight" Value="45"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="*" Binding="{Binding PERMISSION,Mode=OneWay,Converter={StaticResource ResourceKey=up}}" Header="{Binding [User Permissions], Source={x:Static loc:LanguageManager.Instance}}"  >
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Left" />
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontSize" Value="24"/>
                                        <Setter Property="Foreground" Value="{DynamicResource txtColor1}"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Padding" Value="45,25,0,0"/>
                                        <Setter Property="LineHeight" Value="45"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <StackPanel Grid.Column="5" Focusable="False" HorizontalAlignment="Right">
                    <StackPanel.Resources>                         
                        <Style TargetType="Label">
                            <Setter Property="Margin" Value="0,18,0,0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="FontSize" Value="22"/>
                            <Setter Property="Foreground" Value="{DynamicResource txtColor1}"/>
                        </Style>
                        <Style TargetType="Image">
                            <Setter Property="Height" Value="63"/>
                            <Setter Property="Stretch" Value="Uniform"/>
                            <Setter Property="Margin" Value="0,18,0,0"/>
                        </Style>
                       
                    </StackPanel.Resources>
                    <Button Style="{DynamicResource btnStyle}" x:Name="b1" Command="{Binding Add}">
                        <Button.Content>
                            <StackPanel Width="{Binding ElementName=b1,Path=ActualWidth}" Height="{Binding ElementName=b1,Path=ActualHeight}">
                                <Image Source="/SDBS3000;component/Utils/Icon/Add.png"/>
                                <Label Content="{Binding [add], Source={x:Static loc:LanguageManager.Instance}}"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <Button Style="{DynamicResource btnStyle}"  Command="{Binding Modify}">
                        <Button.Content>
                            <StackPanel Width="{Binding ElementName=b1,Path=ActualWidth}" Height="{Binding ElementName=b1,Path=ActualHeight}">
                                <Image  Source="/Utils/Icon/Correct.png" />
                                <Label Content="{Binding [modify], Source={x:Static loc:LanguageManager.Instance}}"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <Button Style="{DynamicResource btnStyle}"  Command="{Binding Save}" IsEnabled="{Binding IsEditing}">
                        <Button.Content>
                            <StackPanel Width="{Binding ElementName=b1,Path=ActualWidth}" Height="{Binding ElementName=b1,Path=ActualHeight}">
                                <Image Source="/SDBS3000;component/Utils/Icon/Save.png" />
                                <Label Content="{Binding [save], Source={x:Static loc:LanguageManager.Instance}}"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <Button  Style="{DynamicResource btnStyle}"   Command="{Binding Delete}" IsEnabled="{Binding IsEditing,Converter={StaticResource GetNegateBool}}">
                        <Button.Content>
                            <StackPanel Width="{Binding ElementName=b1,Path=ActualWidth}" Height="{Binding ElementName=b1,Path=ActualHeight}">
                                <Path Data="{DynamicResource dele}" Stretch="Uniform" Width="60" Margin="0,20,0,0" Fill="#37c2fe"/>
                                <Label Content="{Binding [delete], Source={x:Static loc:LanguageManager.Instance}}"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </StackPanel>

                <Grid Grid.Column="3" x:Name="t3" >
                    <Grid.Resources>
                        <Style TargetType="Label">
                            <Setter Property="FontSize" Value="20"/>
                            <Setter Property="Foreground" Value="{DynamicResource RecColor}"/>
                            <Setter Property="Margin" Value="8,0,0,0"/>
                        </Style>
                        <Style TargetType="Rectangle">
                            <Setter Property="Width" Value="6"/>
                            <Setter Property="Height" Value="22"/>
                            <Setter Property="Fill" Value="{DynamicResource RecColor}"/>
                            <Setter Property="RadiusX" Value="3"/>
                            <Setter Property="RadiusY" Value=" 3"/>
                        </Style>
                        <Style TargetType="uc1:CareTextBox">
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="FontSize" Value="24"/>
                            <Setter Property="Foreground" Value="{DynamicResource txtColor1}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="20,0,0,0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="CaretBrush" Value="{DynamicResource txtColor1}"/>
                        </Style>
                    </Grid.Resources>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="46*"/>
                        <RowDefinition Height="41*"/>
                        <RowDefinition Height="70*"/>
                        <RowDefinition Height="72*"/>
                        <RowDefinition Height="245*"/>
                        <RowDefinition Height="61*"/>
                        <RowDefinition Height="41*"/>
                        <RowDefinition Height="70*"/>
                        <RowDefinition Height="40*"/>
                    </Grid.RowDefinitions>
                    <UniformGrid Columns="3" Grid.Row="1">
                        <WrapPanel>
                            <Rectangle/>
                            <Label Content="{Binding [User Name], Source={x:Static loc:LanguageManager.Instance}}"/>
                        </WrapPanel>
                        <WrapPanel Grid.Column="1">
                            <Rectangle/>
                            <Label Content="{Binding [Password], Source={x:Static loc:LanguageManager.Instance}}"/>
                        </WrapPanel>
                        <WrapPanel Grid.Column="2">
                            <Rectangle/>
                            <Label Content="{Binding [User Permissions], Source={x:Static loc:LanguageManager.Instance}}"/>
                        </WrapPanel>
                    </UniformGrid>
                    <UniformGrid Columns="3" Grid.Row="2" >
                        <uc1:CareTextBox Width="232" Height="70" Background="{DynamicResource userinput1}" Text="{Binding User.NAME}" IsEnabled="{Binding IsEditing}" PreviewKeyDown="TextBox_PreviewKeyDown"/>
                        <uc1:CareTextBox x:Name="ttt" Width="232" Height="70" Background="{DynamicResource userinput1}" Text="{Binding User.PSD}"  IsEnabled="{Binding IsEditing}" InputMethod.IsInputMethodEnabled="False" PreviewKeyDown="TextBox_PreviewKeyDown"/>
                        <ComboBox x:Name="ttt2" Width="232" FontSize="20" Height="70" Style="{DynamicResource ComboBoxStyle2}" SelectedIndex="{Binding User.PERMISSION,Mode=TwoWay}" ItemContainerStyle="{DynamicResource ComboBoxItemStyle2}" IsEnabled="{Binding IsEditing}">
                            <ComboBoxItem Content="{Binding [Administrator], Source={x:Static loc:LanguageManager.Instance}}"/>
                            <ComboBoxItem Content="{Binding [Inspector], Source={x:Static loc:LanguageManager.Instance}}"/>
                            <ComboBoxItem Content="{Binding [Operator], Source={x:Static loc:LanguageManager.Instance}}"/>
                        </ComboBox>
                    </UniformGrid>
                    <WrapPanel Grid.Row="3" Margin="0,25,0,0">
                        <Rectangle/>
                        <Label Content="{Binding [Note], Source={x:Static loc:LanguageManager.Instance}}"/>
                    </WrapPanel>
                    <!--Text="{Binding User.NOTE}"-->
                    <uc1:CareTextBox Grid.Row="4" TextWrapping="Wrap" Padding="20,20,20,0" VerticalContentAlignment="Top" Width="{Binding ElementName=t3,Path=ActualWidth}" Background="{DynamicResource userinput2}" Text="{Binding User.NOTE}"  IsEnabled="{Binding IsEditing}"/>
                    <Image Grid.Row="5" Width="584" Source="/SDBS3000;component/Utils/Image/User/分割线.png"/>
                    <UniformGrid Columns="2" Grid.Row="6">
                        <WrapPanel>
                            <Rectangle/>
                            <Label Content="{Binding [mes1], Source={x:Static loc:LanguageManager.Instance}}"/>
                        </WrapPanel>
                        <WrapPanel>
                            <Rectangle/>
                            <Label Content="{Binding [mes2], Source={x:Static loc:LanguageManager.Instance}}"/>
                        </WrapPanel>
                    </UniformGrid>
                    <UniformGrid Columns="2" Grid.Row="7">
                        <uc1:CareTextBox Width="350" Height="70" Background="{StaticResource userinput3}" Text="{Binding User.MESNAME}"  IsEnabled="{Binding IsEditing}" PreviewKeyDown="TextBox_PreviewKeyDown"/>
                        <uc1:CareTextBox Width="350" Height="70" Background="{StaticResource userinput3}" Text="{Binding User.MESPSD}"  IsEnabled="{Binding IsEditing}"  InputMethod.IsInputMethodEnabled="False" PreviewKeyDown="TextBox_PreviewKeyDown"/>
                    </UniformGrid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
