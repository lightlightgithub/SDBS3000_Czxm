<Page x:Class="SDBS3000.Views.PageWarn"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:loc="clr-namespace:SDBS3000.Resources"
      xmlns:local="clr-namespace:SDBS3000.Views"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      DataContext="{Binding Source={StaticResource Locator},Path=Main}"
      mc:Ignorable="d" 
      d:DesignHeight="880" d:DesignWidth="1920"
      Title="PageWarn">
    <Grid>
        <Border x:Name="main" Width="1730" Height="680" CornerRadius="20">
            <Border.Background>
                <ImageBrush ImageSource="{DynamicResource mainbk}"/>
            </Border.Background>
            <StackPanel>
                <DataGrid x:Name="datagrid" MaxWidth="1700" MaxHeight="700" FontSize="20" BorderBrush="Transparent" Margin="10,0,0,0"
                        AutoGenerateColumns="False" IsReadOnly="True"  HorizontalAlignment="Left" HeadersVisibility="All" VerticalAlignment="Top" CanUserAddRows="False" 
                        HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"  AlternatingRowBackground="{DynamicResource  tipBg3}" RowBackground="{DynamicResource  tipBg4}" RowHeaderWidth="0"
                        GridLinesVisibility="Horizontal" Padding="10,10,0,0" HorizontalGridLinesBrush="Transparent" VerticalGridLinesBrush="Transparent" RowHeight="35"
                        ItemsSource="{Binding WarnList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                        VerticalContentAlignment="Center" SelectionMode="Extended" Style="{DynamicResource DataGridStyle1}">
                    <DataGrid.Resources>
                        <Style TargetType="ScrollBar" BasedOn="{StaticResource ScrollStyle_Mobile}"></Style>
                        <!--纵向滚动条宽度-->
                        <sys:Double   x:Key="VerticalScrollBarThickness">12</sys:Double>
                        <!--纵向滑块圆角-->
                        <CornerRadius x:Key="VerticalThumbCornerRadius"  >5</CornerRadius>
                        <!--横向滚动条宽度-->
                        <sys:Double   x:Key="HorizontalScrollBarThickness">12</sys:Double>
                        <!--横向滑块圆角-->
                        <CornerRadius x:Key="HorizontalThumbCornerRadius"  >5</CornerRadius>
                    </DataGrid.Resources>
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="FontSize" Value="18"/>
                            <Setter Property="Padding" Value="20,0,40,0"/>
                            <Setter Property="Foreground" Value="{DynamicResource txtColor1}"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="LightBlue"/>
                            <Setter Property="BorderThickness" Value="0.5"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="20,0,20,0"/>
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="BorderBrush" Value="LightBlue"/>
                            <Setter Property="BorderThickness" Value="0.5"/>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource txtColor1}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn  Header="{Binding [date], Source={x:Static loc:LanguageManager.Instance}}" Binding="{Binding Date}" Foreground="{DynamicResource txtColor1}" Width="300"/>
                        <DataGridTextColumn x:Name="d1" Width="300" Header="{Binding [time], Source={x:Static loc:LanguageManager.Instance}}" Binding="{Binding Time}" />
                        <DataGridTextColumn  Header="{Binding [Alades], Source={x:Static loc:LanguageManager.Instance}}" Binding="{Binding WarnDescription}" Width="1050"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
